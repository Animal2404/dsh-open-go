# v0.9.3 发布记录

| 项 | 值 |
|---|---|
| 仓库 | `https://github.com/Animal2404/dsh-open-go` |
| 改动 | 开合时序：**先展开、后刷新**，并把「点击 → 面板可见」从 **367ms 压到 19ms** |
| 真因 | 面板 28ms 已挂载，但 `visibility` 挂在 `panelPos` 上，而 `panelPos` 要等量完**面板高度**才好定位；那一次测量没落地，直到刷新响应回来重渲染才补上 —— 于是"看起来像在等刷新" |
| 修法 | ① 展开前只量 pill，用 `bottom` 贴 pill 上沿定位（不依赖面板高度）；② `render = open || closing`，展开与挂载同一帧；③ 刷新挪到**上屏后下一帧**才发（rAF → setTimeout 0）；④ 数据已在手上时展开直接显示真值 |
| 版本 | `package.json` 0.9.2 → 0.9.3；`lib/client.js` / `lib/index.js` 头注释同步 |
| 验证 | 页面内 rAF 逐帧采样 + `performance` 资源计时：挂载 19ms、**可见 19ms**、刷新请求起点 **+24ms**、首帧数值 `7%,20%,2%`（真值）、进场动画 200ms；其余探针不变（面板 280×201、账单开 429、溢出 0、账单默认 0 卡、弹窗居中等） |
| 探针 | `.verify/probes-v0.9.3.json` |
